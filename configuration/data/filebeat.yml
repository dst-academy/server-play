filebeat.prospectors:
- input_type: log
  paths:
    - /var/lib/dsta/cluster/overworld/server_chat_log.txt
  backoff_factor: 1

output.redis:
  hosts: ["${BUFFER_HOST}"]
  password: "${BUFFER_PASSWORD}"
  key: "${BUFFER_KEY}"
  db: ${BUFFER_DATABASE}
  timeout: 5
